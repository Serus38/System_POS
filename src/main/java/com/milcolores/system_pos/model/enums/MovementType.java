package com.milcolores.system_pos.model.enums;

public enum MovementType {

    IN, // IN for incoming movements, e.g. purchase, return, etc.
    OUT, // OUT for outgoing movements, e.g. sale, return, etc.
    ADJUSTMENT // ADJUSTMENT for inventory adjustments, e.g. stock count, damage, etc.

}
